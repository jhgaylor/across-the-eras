apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: across-the-eras-tls
  namespace: across-the-eras
spec:
  secretName: across-the-eras-tls
  issuerRef:
    name: letsencrypt-production
    kind: ClusterIssuer
  dnsNames:
    - eras.inevitable.fyi
